mqtt:
  broker-addr: localhost
  broker-port: 1883
  qos-level: 1

logging:
  level:
    root: info

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/handcontrol_db
    username: handcontrol_user
    password: elephant
  jpa:
    generate-ddl: true

server:
    port: 8081

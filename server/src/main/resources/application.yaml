mqtt:
  broker-addr: localhost
  broker-port: 1883
  qos-level: 1

logging:
  level:
    root: info

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/handcontrol_db
    username: handcontrol_user
    password: elephant
  jpa:
    generate-ddl: true

redis:
  host: 127.0.0.1
  port: 6379

server:
    port: 8081
